import React from "react";
import { Link } from "react-router-dom";

const LogoComponent = () => {
    return (
        <h1>
            <Link to="/">Logo</Link>
        </h1>
    );
};

export default LogoComponent;
